import { css } from '@emotion/react';

export const verticalCenter = css({
    position: 'absolute',
    top: '50%',
});

export const mobileSticky = css({
    position: 'sticky',
    top: '56px',
    zIndex: '3',
});

export const maxSearchSize = css({
    maxWidth: '1400px',
});