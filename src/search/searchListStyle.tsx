import { css } from '@emotion/react';

export const result = css({
    color: '#fff',
    fontWeight: 'bold',
    textAlign: 'center',
});

export const hitCount = css({
    color: '#f00',
    fontSize: '18px',
});

export const verticalCenter = css({
    position: 'absolute',
    top: '50%',
});
