import { css } from "@emotion/react";

export const dataWidth = css({
    width: '100%'
});

export const topIcon = css({
    color: '#fff',
    right: '20px',
    position: 'fixed',
});

export const card = css({
    backgroundColor: '#3C3B40',
    boxSizing: 'border-box',
    padding: '8px',
    margin: '8px',
});